apiVersion: v1
kind: Service
metadata:
  name: ecoloop-ai-service
  labels:
    app: ecoloop-ai
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: ecoloop-ai

