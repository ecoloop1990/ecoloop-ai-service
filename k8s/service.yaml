apiVersion: v1
kind: Service
metadata:
  name: ecoloop-ai-service
  namespace: default
  labels:
    app: ecoloop-ai-service
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: ecoloop-ai-service
